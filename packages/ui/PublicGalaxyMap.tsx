import * as React from "react";
import { PublicGalaxy } from "@twilight-assembly/core";

// const DynamicMap = dynamic(() => import("./mapviewer"), {
//   ssr: false,
// });

export const PublicGalaxyMap = (publicGalaxy: PublicGalaxy) => {
  return <button>Public galaxy Map</button>;
};
