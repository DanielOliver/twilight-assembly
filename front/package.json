{
  "private": true,
  "scripts": {
    "postinstall": "dotnet tool restore",
    "start": "cross-env NODE_OPTIONS=--openssl-legacy-provider dotnet fable watch src --run webpack-dev-server",
    "build": "cross-env NODE_OPTIONS=--openssl-legacy-provider dotnet fable src && cross-env NODE_OPTIONS=--openssl-legacy-provider webpack"
  },
  "devDependencies": {
    "webpack": "^4.46.0",
    "webpack-cli": "^3.3.0",
    "webpack-dev-server": "^3.11.2",
    "cross-env": "^7.0.3",
    "css-loader": "^5.2.7",
    "style-loader": "^2.0.0"
  },
  "dependencies": {
    "bootstrap": "^5.1.3",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "version": "1.0.0"
}
